// a loop is a programming tool that repeats a set of instructions until
// a specified condition called a stopping condition is receached.

// syntax

// for(initialization, stoppingCondition, iterationStatement)
// example
for(let step = 0; step < 5; step++){
    console.log("one step to the left");
}